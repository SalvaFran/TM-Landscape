
---

# ✅ **umap_environment.yml**

This is the minimal and correct GPU environment compatible with CUDA 12.2 and cuML 24.06 (which supports Python 3.10):

```yaml
name: umap-gpu
channels:
  - rapidsai
  - conda-forge
  - nvidia
  - defaults

dependencies:
  - python=3.10
  - cudatoolkit
  - cudatoolkit-dev
  - cupy
  - cuml=24.06
  - numpy
  - scipy
  - pandas
  - pip

